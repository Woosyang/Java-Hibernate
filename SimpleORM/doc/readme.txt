1. create the DB.properties under the src folder, the name of the properties can't 
   be changed.
2. each table in the database can only have one primary key
3. In the PO package, all the fields in the java class better need to use wrapper 
   classes to declare themselves rather than primitive data type 
4. the letter case of the parameters in SQL query must be the same to the column name
   of the table